{% extends 'template.twig' %}
{% import 'component.twig' as component %}

{% block title %}Accueil{% endblock %}

{% block content %}
	<div class="col-sm-6 col-md-4">
		{{ component.tile('teal', 'users', 'Utilisateurs', 'users') }}
	</div>
	<div class="col-sm-6 col-md-2">
		{{ component.tile('blue', 'tasks', 'Logs', 'events') }}
	</div>
	<div class="col-sm-6 col-md-4">
		{{ component.tile('red', 'ticket', 'Tickets de support', 'tickets') }}
	</div>
	<div class="col-sm-6 col-md-2">
		{{ component.tile('silver', 'wrench', 'Config', 'config') }}
	</div>
{% endblock %}