{% import 'component.twig' as component %}
<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{ component.url('/home') }}">{{ name }}</a>
    </div>
    
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse">
      {% if session.id %}
      <ul class="nav navbar-nav navbar-left">
        <li><a href="{{ component.url('/home') }}"><i class="fa fa-home"></i> Accueil</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="">{{ session.nom|capitalize ~ ' ' ~ session.prenom|upper }}</a></li>
        <li><a href="{{ component.url('/tickets/new') }}">Nouveau ticket</a></li>
        <li><a href="{{ component.url('/logout') }}"><i class="fa fa-chain-broken"></i> Déconnexion</a></li>
      </ul>
      {% else %}
      <ul class="nav navbar-nav navbar-right">
        <li><a href="{{ component.url('/tickets') }}">Mes tickets</a></li>
        <li><a href="{{ component.url('/tickets/new') }}">Nouveau ticket</a></li>
        <li><a href="{{ component.url('/connexion') }}">Visiteur</a></li>
      </ul>
      {% endif %}
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>